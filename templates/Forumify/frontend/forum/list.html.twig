{% extends '@!Forumify/frontend/forum/list.html.twig' %}
{# Keep the original forum list. The extends declaration must be on the first
   line because Forumify's theme loader replaces that line when chaining themes.
   This file also replaces the legacy sample during ZIP-based upgrades. #}
