/tests/vendor/
/tests/composer.lock

# Auto-saved from pasted chat images - reference material only, never shipped (see spec
# section 29 in the design conversation: the mockup screenshot itself must not become a
# theme asset).
/design-reference/
